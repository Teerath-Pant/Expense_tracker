{
  "name": "expense-tracker-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monolithic Expense Tracker with React frontend and Express/oRPC backend",
  "scripts": {
    "dev:web": "npm run dev --prefix app/web",
    "dev:server": "npm run dev --prefix app/server",
    "dev": "npx concurrently --kill-others \"npm run dev:server\" \"npm run dev:web\"",
    "build:web": "npm run build --prefix app/web",
    "build:server": "npm run build --prefix app/server",
    "build": "npm run build:server && npm run build:web"
  }
}
